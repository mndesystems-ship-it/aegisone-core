| test name | invariant | artifact used | pass condition | failure example |
| --- | --- | --- | --- | --- |
| replay_consistency | identical input gives identical output | `stable-proof-bundle/receipts.jsonl`, `audit-proof-bundle/determinism_proof.json`, `audit-proof-bundle/proof_bundle/replay_results.json` | determinism mismatch count `0`, replay drift count `0`, replay mismatch array empty | altered receipt stream in `audit-proof-bundle/proof_bundle/failure_corrupted_entry.jsonl` triggers replay mismatch |
| concurrency_storm | one execution id produces one winner and one refusal path | `post-remediation-verification-bundle/post_verification_report.json` | duplicate allows `0`, winner count `100`, loser count `100`, refusal code `ERR_EXECUTION_ID_ALREADY_CONSUMED` | TC-056 duplicate execution id forces second claimant to refuse |
| malformed_input | malformed or ambiguous inputs fail closed with a single primary reason | `audit-proof-bundle/schema_enforcement_report.json`, `attack-wave-bundle/attack_wave_results.json` | incorrect accept rate `0`, target full rejection `true`, regression cases stay `PASS` | TC-046 invalid numeric payload refuses with `ERR_INVALID_JSON_NUMBER` |
| policy_trust | policy version and signature trust mismatches refuse before execution | `remediation-wave-bundle/remediation_wave_results.json` | TC-068, TC-069, and TC-070 all `PASS` with expected reason codes | TC-070 mismatched signed policy refuses with `ERR_INVALID_POLICY_SIGNATURE` |
| proof_bundle_check | the core proof claims remain intact across audit and controlled benchmark runs | `audit-proof-bundle/summary.json`, `audit-proof-bundle/proof_bundle/parity_report.json`, `mnde-controlled-benchmark-bundle/summary.json` | rejection accuracy `100`, parity mismatch count `0`, zero drift `true`, zero replay mismatch `true`, cost reduction `98` | partial receipt stream in `audit-proof-bundle/proof_bundle/failure_partial_write.jsonl` fails closed during replay |
| repeatability_capture | rerunning stable mode yields byte-identical core artifacts | `stable-proof-bundle/manifest.json`, `stable-proof-bundle/receipts.jsonl`, `external-review-drop/runs/*/recursive_hashes.json` | stable bundle byte equality `true`, stable manifest hash match `true`, stable receipts hash match `true` | any changed byte in `stable-proof-bundle` produces a recursive hash mismatch |
| scoped_cost_policy | projected cost over scope refuses with explicit cost limit reason | `mnde-controlled-benchmark-bundle/summary.json`, `BUSINESS_PROOF.md` | scoped requests above `$100` projected cost refuse and prevent spend | runaway autoscale case refuses with `ERR_COST_LIMIT` instead of incurring spend |
