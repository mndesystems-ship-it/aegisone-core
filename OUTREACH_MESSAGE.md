The attached package contains a deterministic execution control proof bundle for a `Preflight -> Orbit -> ARM -> RAM0NA` decision path that returns `ALLOW` or `REFUSE` before execution.

It matters because it shows zero-drift determinism, exact replay, fail-closed adversarial behavior, byte-identical Node/Rust parity, and reproducible cost prevention on real refusal cases.

Proof package: `technical-drop-package.zip`

Please try to break determinism, replay, or parity.
